# Исключаем Git-репозиторий
.git
.gitignore

# Исключаем Docker-файлы (если не нужны внутри контейнера)
.dockerignore
Dockerfile

# Исключаем IDE-шные и редакторские конфиги
*.swp
*.swo
*.idea/
.vscode/
*.code-workspace

# Исключаем временные и кешированные файлы
*.log
*.tmp
*.out
*.exe
*.test

# Исключаем зависимости и кэш
vendor/
node_modules/
.env
.env.*
*.local

# Исключаем директории тестов (если не нужны в образе)
test/
tests/

# Исключаем билд-артефакты
bin/
build/
dist/

# Исключаем всё что создаётся во время сборки
*.o
*.a
*.so
